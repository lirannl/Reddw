{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "ui:dev",
            "type": "npm",
            "script": "dev",
            "problemMatcher": [],
            "isBackground": true,
            "options": {
                "cwd": "${workspaceFolder}"
            }
        },
        {
            "label": "build",
            "type": "shell",
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "options": {
                "cwd": "${workspaceFolder}/src-tauri"
            },
            "command": "npm build && cargo build -r",
        }
    ]
}